export interface taskModel {
  id: string;
  title: string;
  summary: string;
  priority: string;
  dueDate: string;
}

export interface priorityModel {
  name: string;
  id: string;
  color: string;
}
